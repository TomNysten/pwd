{% extends './layout.html.twig' %}

{% block title %}Edit Comments{% endblock %}

{% block content %}

    <!-- Page Heading -->
    <div class="page-heading">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <h1>Edition - commentaire</h1>
                    <p>Vous pouvez ici éditer le commentaire.</p>
                </div>
            </div>
        </div>
    </div>

    <div class="row" style="padding-bottom: 50px; border-bottom: 2px solid #eee; margin-bottom: 10px;">
        <div class="col-md-12">
            {{ include('admin/comments/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 primary-button">
            <a href="{{ path('comments_index') }}">back to list</a>
        </div>
        <div class="col-md-6">
            {{ include('admin/comments/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
